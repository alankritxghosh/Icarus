# Icarus brain — Python dependencies.
#
# The core harness/demo are pure stdlib. The ONE exception is semantic
# retrieval (Brick C), which uses a local, free, offline embedding model via
# fastembed (ONNX Runtime + tokenizers, NO PyTorch). It is lazily imported
# inside evals.provider.LocalEmbeddingProvider, so everything else still runs
# without it installed; installing this is what enables semantic/hybrid
# retrieval. Default model BAAI/bge-small-en-v1.5, fetched once and cached.
fastembed==0.8.0
