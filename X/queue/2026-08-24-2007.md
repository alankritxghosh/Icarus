# Queue — 2026-08-24-2007

## Candidate 1 — Track A (evidence reply)

- **Parent:** https://x.com/nicknisi/status/2091918520791494912
- **Author:** @nicknisi (Nick Nisi) — real individual engineer, shipping a personal
  open-source CLI/MCP tool (`sessions`), not a growth/hype account.
- **Parent text:** announcing new features in `sessions`, his CLI/MCP for recalling
  AI coding sessions across tools. Headline feature: `sessions why <file[:line]|commit>`
  resolves the commit that touched a piece of code and correlates it to the AI
  session that produced it, to answer "why does this code exist?"
- **Parent reach:** 171 views, 6 likes, 4 replies (at time of scouting)
- **Parent age:** ~4-5h old at scouting time
- **Why this parent:** small, uncrowded (4 replies), a real engineer working the
  adjacent problem (resolving "why does this code exist" from git history), and
  the mechanism has a genuine, non-competitive blind spot our own measurement
  already proved: correlating to the commit that touched the code only works
  when a commit exists. A closed-unmerged PR leaves none.

### Draft
sessions why finds the commit that touched code, so it finds merges. It misses a PR that tried the fix and closed unmerged, since that leaves no commit. We measured it: an agent reading git log alone called a bug fixed off the merge, missing PR #20754, a rejected second attempt.

- **Char count:** 279
- **Lint:** PASS (one warning only: "one block" — hard line breaks stylistic note, not a hard fail)
- **Source for the number:** `docs/experiments/2026-08-10-agent-mode-exp-d-efficiency.md`
  — control agent (git log only) found merged commit `8d09b838` / PR #20752 and
  declared the bug fixed; missed PR #20754 (a second, closed-unmerged attempt at
  the same issue) which only the Icarus-assisted arm surfaced. Inventory item 2
  in `content-pillars.md`.
- **Note:** this is thematically adjacent to a directly-competing product space
  (session/commit-correlation "why" tooling). Judged safe to engage because (a)
  the reply adds a genuinely different finding (rejected-attempt blindness) not
  a comparison or pitch, (b) no link, no product name, (c) small/low-stakes
  parent (171 views), not a large audience being poached. If this reads as too
  close to poaching on review, skip and log it.

**STATUS: SENT** — Alankrit posted this 2026-08-24 ~19:30 UTC (x.com/alankritxghosh/status/2091984169895330081). See X Replies.md batch 4 in the vault for results.
